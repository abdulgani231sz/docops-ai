import {list,failure} from '@/lib/storage';import {csvCell,issuesFor} from '@/lib/documents';
export async function GET(){try{const all=await list();const docs=all.filter(d=>d.kind==='invoice'&&d.status==='approved'&&issuesFor(d,all).length===0);const rows=[['Invoice','Vendor','PO','Date','Currency','Subtotal','Tax','Total'],...docs.map(d=>[d.fields.number,d.fields.vendor,d.fields.poNumber,d.fields.date,d.fields.currency,d.fields.subtotal,d.fields.tax,d.fields.total])];return new Response(rows.map(r=>r.map(csvCell).join(',')).join('\r\n'),{headers:{'Content-Type':'text/csv; charset=utf-8','Content-Disposition':'attachment; filename="docops-approved.csv"','Cache-Control':'no-store'}})}catch(e){return failure(e)}}
