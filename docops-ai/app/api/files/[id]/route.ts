import {list,bucket,json,failure} from '@/lib/storage';
export async function GET(r:Request,{params}:{params:Promise<{id:string}>}){try{const {id}=await params;const d=(await list()).find(d=>d.id===id);if(!d?.fileKey)return json({error:'Original file not found.'},404);const f=await bucket().get(d.fileKey);if(!f)return json({error:'Original file is unavailable.'},404);return new Response(f.body,{headers:{'Content-Type':f.httpMetadata?.contentType||'application/octet-stream','Content-Disposition':`inline; filename*=UTF-8''${encodeURIComponent(d.filename)}`,'Cache-Control':'private, no-store','X-Content-Type-Options':'nosniff','Content-Security-Policy':"sandbox"}})}catch(e){return failure(e)}}
